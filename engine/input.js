export class Input { constructor(){ this.keys = new Set(); this.lastKey = null; window.addEventListener('keydown', e=>{ this.keys.add(e.key.toLowerCase()); this.lastKey = e.key.toLowerCase(); }); window.addEventListener('keyup', e=>{ this.keys.delete(e.key.toLowerCase()); }); } down(k){ return this.keys.has(k); } consume(k){ if(this.lastKey === k){ this.lastKey = null; return true; } return false; } }