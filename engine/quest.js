export class QuestManager { constructor(){ this.quests = new Map(); } addQuest(id, title, steps){ this.quests.set(id,{id,title,steps,status:'active',progress:0}); } progress(id){ const q = this.quests.get(id); if(!q||q.status!=='active') return; q.progress++; if(q.progress >= q.steps){ q.status='completed'; } } renderText(){ const lines=[]; for(const q of this.quests.values()){ lines.push(`${q.title} [${q.status}] ${q.progress}/${q.steps}`); } return lines.join('\n'); } }